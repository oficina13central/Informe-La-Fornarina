{
  "name": "chazarreta-reportes",
  "version": "1.0.0",
  "private": true,
  "description": "Informe mensual imprimible para comandas de Google Sheets.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
